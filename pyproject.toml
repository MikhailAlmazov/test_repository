[tool.poetry]
name = "test-repository"
version = "0.1.0"
description = ""
authors = ["MikhailAlmazov <almazovmikhail@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
